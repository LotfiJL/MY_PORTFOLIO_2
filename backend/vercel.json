{
  "version": 2,
  "builds": [{ "src": "backend/server.js", "use": "@vercel/node" }],
  "routes": [
    { "src": "/submit-form", "dest": "/backend/server.js" }, 
    { "src": "/(.*)", "dest": "/" }
  ]
}
